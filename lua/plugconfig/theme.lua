require("onedarkpro").setup(
  {
    hlgroups = {
      TermCursor = {bg = "${white}"}
    },
    options = {
      cursorline = true,
      window_unfocussed_color = true
    }
  }
)
require("onedarkpro").load()
