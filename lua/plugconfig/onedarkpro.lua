require("onedarkpro").setup(
    {
        caching = true,
        highlights = {
            TermCursor = {bg = "${white}"}
        },
        options = {
            cursorline = true,
            highlight_inactive_windows = true
        }
    }
)
